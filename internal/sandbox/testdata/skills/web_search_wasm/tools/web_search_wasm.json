{
  "name": "web_search_wasm",
  "description": "Search the web using DuckDuckGo. Returns titles, URLs, and snippets. Runs in a WASM sandbox with no external dependencies.",
  "parameters": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query"
      },
      "max_results": {
        "type": "integer",
        "description": "Maximum number of results to return (default: 5)"
      }
    },
    "required": ["query"]
  }
}
